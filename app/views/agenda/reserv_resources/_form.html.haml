= form_for @agenda_reserv_resource do |f|
  - if @agenda_reserv_resource.errors.any?
    #error_explanation
      %h2= "#{pluralize(@agenda_reserv_resource.errors.count, "error")} prohibited this agenda_reserv_resource from being saved:"
      %ul
        - @agenda_reserv_resource.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :reservation_id
    = f.number_field :reservation_id
  .field
    = f.label :resource_id
    = f.number_field :resource_id
  .actions
    = f.submit 'Save'
