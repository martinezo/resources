= form_for @catalogs_status do |f|
  - if @catalogs_status.errors.any?
    #error_explanation
      %h2= "#{pluralize(@catalogs_status.errors.count, "error")} prohibited this catalogs_status from being saved:"
      %ul
        - @catalogs_status.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
