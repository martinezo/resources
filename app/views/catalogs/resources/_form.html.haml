= form_for @catalogs_resource do |f|
  - if @catalogs_resource.errors.any?
    #error_explanation
      %h2= "#{pluralize(@catalogs_resource.errors.count, "error")} prohibited this catalogs_resource from being saved:"
      %ul
        - @catalogs_resource.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :abbr
    = f.text_field :abbr
  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :resource_type_id
    = f.number_field :resource_type_id
  .field
    = f.label :description
    = f.text_field :description
  .field
    = f.label :location
    = f.text_field :location
  .field
    = f.label :active
    = f.check_box :active
  .field
    = f.label :admin_user_id
    = f.number_field :admin_user_id
  .field
    = f.label :unique
    = f.check_box :unique
  .actions
    = f.submit 'Save'
