= form_for @catalogs_department do |f|
  - if @catalogs_department.errors.any?
    #error_explanation
      %h2= "#{pluralize(@catalogs_department.errors.count, "error")} prohibited this catalogs_department from being saved:"
      %ul
        - @catalogs_department.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :abbr
    = f.text_field :abbr
  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :img_header
    = f.text_field :img_header
  .field
    = f.label :institution_id
    = f.number_field :institution_id
  .actions
    = f.submit 'Save'
