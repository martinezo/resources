= form_for @catalogs_institution do |f|
  - if @catalogs_institution.errors.any?
    #error_explanation
      %h2= "#{pluralize(@catalogs_institution.errors.count, "error")} prohibited this catalogs_institution from being saved:"
      %ul
        - @catalogs_institution.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :abbr
    = f.text_field :abbr
  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
