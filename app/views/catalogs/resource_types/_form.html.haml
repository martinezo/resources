= form_for @catalogs_resource_type do |f|
  - if @catalogs_resource_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@catalogs_resource_type.errors.count, "error")} prohibited this catalogs_resource_type from being saved:"
      %ul
        - @catalogs_resource_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_field :description
  .actions
    = f.submit 'Save'
