- if @catalogs_resource_type.errors.count > 0
  $('#new-update-dialog').empty();
  $('#new-update-dialog').html("#{escape_javascript(render(partial: 'form', layout: 'layouts/modal', locals: {title: t('titles.resource_type')} ))}");
- else
  $('#records').empty();
  $('#records').html("#{escape_javascript(render 'records')}");
  $('#new-update-dialog').modal('toggle');